{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug Current C++ File",
            "type": "lldb",
            "request": "launch",
            "program": "${fileDirname}/${fileBasenameNoExtension}",
            "cwd": "${workspaceFolder}",
        }
    ]
}
